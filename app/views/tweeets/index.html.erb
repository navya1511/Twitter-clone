<section class="section">
  <div class="container">
    <div class="columns">
    <%# Trending posts %>
      <% if user_signed_in? %>
        <%= render 'profile' %>
      <% else %>
        <%= render 'trends' %>
      <% end %>

      <%= render 'feed'%>
        
     <%# who to follow %>

      <%= render 'who_to_follow' %>
    
    </div>
  </div>
</section>



    


